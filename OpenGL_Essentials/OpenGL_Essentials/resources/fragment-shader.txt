#version 400 core

uniform sampler2D tex;

in vec3 vPosition;
in vec2 vTexCoord;
in vec3 vNormalColor;

out vec4 vOutputColour;


void main() {

    //note: the texture function was called texture2D in older versions of GLSL
    vOutputColour =  texture(tex, vTexCoord);
}
